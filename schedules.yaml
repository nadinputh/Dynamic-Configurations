prefix: 'schedule:'

schedules:
  - enabled: false
    name: 'notifications'
    cron: '45 * * * * *'
    event: 'notifications'
  - enabled: false
    name: 'every_minute'
    cron: '1 * * * * *'
    event: 'every_minute'
